// ============================================================================
// Smitten — Supabase project config
//
// Fill these in from your Supabase project:
//   Project → Settings → API → "Project URL" and "anon public" key.
//
// The anon key is SAFE to expose in client-side code — it's the public key
// meant for browsers, and every table it can touch is locked down by the
// Row Level Security policies in supabase/schema.sql. NEVER put the
// "service_role" key here or anywhere in this repo.
// ============================================================================
window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
